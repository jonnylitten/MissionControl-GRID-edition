; Grid Autosport (program id 0100dc800a602000) — analog throttle/brake on the
; right stick, with physical right-stick up/down repurposed as digital ZR/ZL
; (handy for gear shifts on the same thumb). Shipped enabled by default.
[trigger_map]
mode=rstick_y_split
stick_y_to_buttons_threshold=50
